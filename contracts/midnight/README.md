# Midnight Phase 3 contract

The authoritative Compact source is `packages/zk/circuits/eligibility.compact`; this copy is kept as a discoverable contract source for the repository structure. Compile from `packages/zk`.
